[package]
name = "pet-tui"
version = "0.1.0"
edition = "2021"
description = "Terminal-based pet companion for open-pets"

[dependencies]
pet-engine = { path = "../pet-engine" }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
